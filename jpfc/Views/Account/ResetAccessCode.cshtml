@using System.Collections.Generic
@using System.Linq
@using Microsoft.AspNetCore.Http
@using Microsoft.AspNetCore.Http.Authentication
@model jpfc.Models.ManageViewModels.AccessCodeViewModel

@{
    ViewData["Title"] = "Change Access Code";
}

<!--Page Title-->
<section class="page-title" style="background-image:url(../../images/banner-images/login-min.jpg)">
    <div class="auto-container">
        <div class="content">
            <h1>@ViewData["Title"]</h1>
            <ul class="page-breadcrumb">
                <li><a asp-controller="Account" asp-action="ManageProfile">Home</a></li>
                <li>@ViewData["Title"]</li>
            </ul>
        </div>
    </div>
</section>
<!--End Page Title-->
<div class="container mt-5">
    <div class="row">
        <div class="col-md-4">
            <section>
                <form asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">

                    <div class="form-group">
                        <label asp-for="CurrentAccessCode"></label>
                        <input asp-for="CurrentAccessCode" class="form-control" />
                        <span asp-validation-for="CurrentAccessCode" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="NewAccessCode"></label>
                        <input asp-for="NewAccessCode" class="form-control" />
                        <span asp-validation-for="NewAccessCode" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">Update Access Code</button>
                        <a class="btn btn-warning" asp-controller="Account" asp-action="ManageProfile">Back</a>
                    </div>
                </form>
            </section>
        </div>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
